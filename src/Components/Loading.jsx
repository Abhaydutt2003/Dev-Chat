//simple component to render loading while the other component is 
//in the suspense state (might be used for other purposes as well)


const Loading = ()=>{
    return <>Loading....</>
};


export default Loading;
